---

sftp_server_name: travisci-sftp.library.ucla.edu

sftp_username: travisci

sftp_path: /build

ansible_ident_file: /home/ansible/.ssh/id_rsa

# Define tomcat instance
#tomcat_inst_name: appname

# Define path to build artifact (relative to sftp_path)
#project_source_path: appname/appname.war

# Define path to deploy build artifact (including file name)
#project_target_path: /usr/local/appname/webapps/appname.war
